<Page x:Class="CourseProject_WPF_.View.AllAnnouncement"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:CourseProject_WPF_.View"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"  
      mc:Ignorable="d"
      d:DesignWidth="800" d:DesignHeight="600"
      Title="AllAnnouncement" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Background="{x:Null}" >

    <Grid >
        <Button x:Name="updateButton"  Width="60" Background="Purple" BorderBrush="{x:Null}" Foreground="White" Click="updateButton_Click" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,10,30,0"  >
            <materialDesign:PackIcon Kind="Restart" Height="25" Width="25" />
        </Button>
        <ListBox x:Name="listView" FontSize="14" Margin="10,70,10,0" HorizontalContentAlignment="Stretch" ItemsSource="{Binding Announcements}" ScrollViewer.VerticalScrollBarVisibility="Disabled">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <DockPanel>
                        <TextBlock x:Name="text" Text="{Binding}" />
                        <Button Content="Подробнее" HorizontalAlignment="Right" VerticalAlignment="Center" Width="100" FontSize="12" Click="listItemButton_Click"></Button>
                    </DockPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</Page>
