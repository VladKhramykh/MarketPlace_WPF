<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:CourseProject_WPF_.Resourses">


    <Style x:Key="TextBoxes" TargetType="TextBox" BasedOn="{StaticResource MaterialDesignFloatingHintTextBox}">        
        <Setter Property="Background" Value="{x:Null}"/>                 
        <Setter Property="SelectionBrush" Value="#FF959595"/>
        <Setter Property="Foreground" Value="LightGray" />
        <Setter Property="Margin" Value="5,0,5,0" />
        <Setter Property="BorderBrush" Value="#FF303032"/>
        <Setter Property="BorderThickness" Value="0,0,0,2"/>        
        <Setter Property="FontSize" Value="18" />       
    </Style>

    <Style x:Key="PasswordBoxes" TargetType="PasswordBox" BasedOn="{StaticResource MaterialDesignFloatingHintPasswordBox}">
        <Setter Property="Background" Value="{x:Null}"/>
        
        <Setter Property="SelectionBrush" Value="#FF959595"/>
        <Setter Property="Foreground" Value="LightGray" />
        <Setter Property="Margin" Value="5,0,5,0" />
        <Setter Property="BorderBrush" Value="#FF303032"/>
        <Setter Property="BorderThickness" Value="0,0,0,2"/>
        <Setter Property="FontSize" Value="18" />
    </Style>
</ResourceDictionary>